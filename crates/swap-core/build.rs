//! Build script for swap-core.
//! UniFFI uses proc-macros, so no UDL file is needed.

fn main() {
    // No-op: Using UniFFI proc-macros instead of UDL
    // The uniffi::setup_scaffolding!() macro in lib.rs handles everything
}
